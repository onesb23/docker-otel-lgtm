receivers:
  datadog:
    endpoint: 0.0.0.0:8126
    read_timeout: 60s

service:
  pipelines:
    metrics:
      receivers: [datadog]
      exporters: [otlphttp/metrics]
    traces:
      receivers: [datadog]
      exporters: [otlphttp/traces]
